# Prod secrets — copy to .env.prod (gitignored) and fill with real values.
# Loaded by `slsv deploy --stage prod`. Falls back to .env for any name not set here.
WEBHOOK_SECRET=CHANGE_ME_prod_webhook_secret
ADMIN_KEY=CHANGE_ME_prod_admin_key
